// Datatypes in C++

/*
 #include <iostream>
 using namespace std;
 
 int main(int argc, char const *argv[])
 {
    int a,b,c;
    cin>>a>>b>>c;

    cout << "the size of a is "<<(a) << "\n";   //displays val of a b c 
    cout << "the size of b is "<<(b) << "\n";
    cout << "the size of c is "<<(c) << "\n\n";

    cout << "the size of int is "<< sizeof(a) << "\n";   // size of is used for no. of bits
    cout << "the size of int is "<< sizeof(b) << "\n";
    cout << "the size of int is "<< sizeof(c) << "\n\n";

    char z;
    cin>>z;
    cout << "the size of char is "<< sizeof(z) << "\n";
    cout << "the size of c is "<<(z)<< "\n\n";

    float f;
    cin>>f;
    cout << "the size of float is "<< sizeof(f) << "\n";
    cout << "the size of f is "<<(f)<< "\n\n";

    double d;
    cin>>d;
    cout << "the size of double is "<< sizeof(d) << "\n";
    cout << "the size of d is "<<(d)<< "\n\n";

    long l;
    cin>>l;
    cout << "the size of long is "<< sizeof(l) << "\n";
    cout << "the size of l is "<<(l)<< "\n\n";

    return 0;
}
*/

/* OUTPUT

22
56
78
the size of a is 22
the size of b is 56
the size of c is 78

the size of int is 4
the size of int is 4
the size of int is 4

C 
the size of char is 1
the size of c is C

77.345363
the size of float is 4
the size of f is 77.3454

3278
the size of double is 8
the size of d is 3278

435
the size of long is 4
the size of l is 435
*/


